name: Security CI

on:
  pull_request:
  push:
    branches: ["main", "master"]
  workflow_dispatch:
  schedule:
    - cron: "15 5 * * 1"

concurrency:
  group: ezsftp-security-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  security-batches:
    name: Security (${{ matrix.batch }})
    runs-on: [self-hosted, Linux, X64, docker]
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        batch: ["secrets", "static"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Secret scan (gitleaks)
        if: matrix.batch == 'secrets'
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install shell tooling
        if: matrix.batch == 'static'
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Shell static security checks
        if: matrix.batch == 'static'
        run: |
          set -euo pipefail
          bash -n ezsftp.sh
          shellcheck ezsftp.sh

  codeql:
    name: CodeQL (actions)
    runs-on: [self-hosted, Linux, X64, docker]
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: actions

      - name: Analyze
        uses: github/codeql-action/analyze@v4
